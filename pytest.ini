[pytest]
# Where pytest looks for tests.
testpaths = tests
# Put the repo root on sys.path so tests can `import server...` without installing the package.
pythonpath = .
# Only collect files/functions matching these patterns (the defaults, stated explicitly).
python_files = test_*.py
python_functions = test_*
